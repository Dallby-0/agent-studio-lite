1. **修改数据库表结构**：
   - 删除现有的6个状态机工作流相关表（state_workflow_definition、state_node、state_transition、state_global_variable、state_workflow_instance、state_execution_log）
   - 重新设计state_workflow_definition表，添加json_definition字段存储完整的工作流定义
   - 保留state_workflow_instance和state_execution_log表，但简化其结构

2. **修改Java实体类**：
   - 更新StateWorkflowDefinition实体，添加jsonDefinition字段
   - 移除StateNode、StateTransition、GlobalVariable实体类
   - 修改StateMachineEngine，使其从JSON解析工作流定义

3. **更新执行逻辑**：
   - 修改loadWorkflowDefinition方法，从JSON解析节点、转换和全局变量
   - 更新相关的Mapper方法，移除对多个表的操作

4. **修改数据库初始化文件**：
   - 更新01-schema.sql，使用新的表结构
   - 更新02-data.sql，插入包含完整JSON定义的工作流示例

5. **确保兼容性**：
   - 保持API接口不变，确保前端可以正常调用
   - 确保工作流执行逻辑保持一致

新的工作流定义JSON格式将包含：
- 全局变量定义
- 节点列表（包含节点类型、配置、位置等）
- 转换规则（包含条件表达式）

这样的设计将简化数据库结构，提高灵活性，便于后端直接解析JSON执行工作流。